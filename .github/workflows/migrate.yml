name: Prisma Migrate (Supabase)

on:
  workflow_dispatch:

jobs:
  migrate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm prisma generate

      - name: Debug DB env (safe)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          node - <<'NODE'
          function show(name) {
            const v = process.env[name] || "";
            console.log(name, "present:", !!v, "len:", v.length);
            if (v) {
              try { console.log(name, "host:", new URL(v).host); }
              catch { console.log(name, "not a valid URL"); }
            }
          }
          show("DATABASE_URL");
          show("DIRECT_URL");
          if (!process.env.DIRECT_URL) {
            console.error("DIRECT_URL is missing/empty. Add it to Repository secrets.");
            process.exit(1);
          }
          NODE

      - name: Network diagnostics (safe)
        env:
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          node - <<'NODE'
          const u = new URL(process.env.DIRECT_URL);
          console.log("HOST:", u.hostname);
          console.log("PORT:", u.port || "(default)");
          NODE

          HOST=$(node -e "console.log(new URL(process.env.DIRECT_URL).hostname)")
          echo "DNS (getent ahosts):"
          getent ahosts "$HOST" || true

          echo "TCP test 5432:"
          nc -vz -w 5 "$HOST" 5432 || true

      # Important: force BOTH env vars to the DIRECT 5432 URL for migrations
      - name: Show migration target (safe)
        env:
          DATABASE_URL: ${{ secrets.DIRECT_URL }}
        run: |
          node -e "console.log('MIGRATION host:', new URL(process.env.DATABASE_URL).host)"

      - name: Migrate deploy (force direct 5432)
        env:
          DATABASE_URL: ${{ secrets.DIRECT_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: pnpm prisma migrate deploy

      - name: Verify tables exist
        env:
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          echo "Connected to:"
          node -e "console.log(new URL(process.env.DIRECT_URL).host)"
          pnpm prisma db execute --url "$DIRECT_URL" --stdin <<'SQL'
          select to_regclass('public._prisma_migrations') as prisma_migrations_table;
          select table_schema, table_name
          from information_schema.tables
          where table_schema='public'
          order by table_name;
          SQL
